# Testing

Run unit tests:

```bash
make test-unit
```

Run integration tests:

```bash
make test-integration
```

The integration tier is intended to demonstrate schema evolution, checkpoint recovery, quarantine replay, and the small Z-order benchmark path.
